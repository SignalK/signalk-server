/** Elapsed time (interval) in seconds (s) */
export type Seconds = number;

/** Frequency in Hertz (Hz) */
export type Hertz = number;

/** Volume in cubic meters (m³) */
export type CubicMeters = number;

/** Liquid or gas flow in cubic meters per second (m³/s) */
export type CubicMetersPerSecond = number;

/** Liquid or gas flow in kilograms per second (kg/s) */
export type KilogramsPerSecond = number;

/** Density in kg per cubic meter (kg/m³) */
export type KilogramsPerCubicMeter = number;

/** Latitude or longitude in decimal degrees (°) */
export type Degrees = number;

/** Angular arc in radians (rad) */
export type Radians = number;

/** Angular rate in radians per second (rad/s) */
export type RadiansPerSecond = number;

/** Electrical current in ampere (A) */
export type Amperes = number;

/** Electrical charge in Coulomb (C) */
export type Coulombs = number;

/** Electrical potential in volt (V) */
export type Volts = number;

/** Power in watt (W) */
export type Watts = number;

/** Torque in Newton meter (Nm) */
export type NewtonMeters = number;

/** Electrical energy in joule (J) */
export type Joules = number;

/** Electrical resistance in ohm (Ω) */
export type Ohms = number;

/** Distance in meters (m) */
export type Meters = number;

/** Speed in meters per second (m/s) */
export type MetersPerSecond = number;

/** (Surface) area in square meters (m²) */
export type SquareMeters = number;

/** Temperature in kelvin (K) */
export type Kelvin = number;

/** Pressure in pascal (Pa) */
export type Pascals = number;

/** Mass in kilogram (kg) */
export type Kilograms = number;

/** Relative value compared to reference or normal value (ratio) */
export type Ratio = number;

/** Acceleration in meters per second squared (m/s²) */
export type MetersPerSecondSquared = number;

/** Angular acceleration in radians per second squared (rad/s²) */
export type RadiansPerSecondSquared = number;

/** Force in newton (N) */
export type Newtons = number;

/** Magnetic field strength in tesla (T) */
export type Tesla = number;

/** Light Intensity in lux (Lux) */
export type Lux = number;

/** Pressure change rate in pascal per second (Pa/s) */
export type PascalsPerSecond = number;

/** Viscosity in pascal seconds (Pa·s) */
export type PascalSeconds = number;
