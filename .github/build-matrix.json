{
  "_comment": "Source of truth for the OS x Node x Arch x Edition combinations the Docker workflows build. See .github/docs/build-matrix.md for field definitions, naming conventions, and how to add new rows. When adding OS or Node variants, also add a workflow_dispatch input to the three Docker workflows; editions are not dispatch inputs and follow their own default_enabled.",
  "architectures": [
    { "id": "amd", "vm": "ubuntu-latest", "platform": "linux/amd64" },
    { "id": "arm", "vm": "ubuntu-24.04-arm", "platform": "linux/arm64" }
  ],
  "os_variants": [
    {
      "id": "24.04",
      "family": "ubuntu",
      "os_arg": "24.04",
      "dockerfile_base": "./docker/Dockerfile_base_24.04",
      "tag_suffix": "",
      "default_enabled": true
    },
    {
      "id": "26.04",
      "family": "ubuntu",
      "os_arg": "26.04",
      "dockerfile_base": "./docker/Dockerfile_base_26.04",
      "tag_suffix": "-26.04",
      "default_enabled": true
    },
    {
      "id": "alpine",
      "family": "alpine",
      "os_arg": "alpine",
      "dockerfile_base": "./docker/Dockerfile_base_alpine",
      "tag_suffix": "",
      "default_enabled": true
    }
  ],
  "node_versions": [
    {
      "id": "24",
      "primary": true,
      "default_enabled": true,
      "extra_platforms": {}
    },
    {
      "id": "26",
      "primary": false,
      "default_enabled": true,
      "extra_platforms": {}
    }
  ],
  "editions": [
    { "id": "full", "tag_suffix": "", "default_enabled": true },
    { "id": "core", "tag_suffix": "-core", "default_enabled": true }
  ]
}
